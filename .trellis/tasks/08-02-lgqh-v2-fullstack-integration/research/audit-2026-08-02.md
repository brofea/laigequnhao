# T05 审计记录（2026-08-02）

## 联合 Review 结论

- 产品：公开端 3 个区域（发现/标签/板块）与详情深链/分享原为 fixture 或模拟，全部接真实 API；管理端分页被 T04 契约变更破坏，已重写为页码契约 + URL 状态。
- Staff：API client 补齐超时/非 JSON/malformed 归一化与 `ClientError.kind`；竞态用 AbortController + URL syncedKey 守卫；adapter 集中在 `src/features/groups/adapters.ts`。
- QA：新增 client 单测 9 例、真实数据 E2E 5 例；管理 50/51、深链、板块成员、编辑持久化、跨页排序均有断言。

## 用户决策记录

1. 本地优先 + Turnstile 旁路（`SKIP_TURNSTILE=true`，placeholder 仅本地，不进生产验收）。
2. 批准 BoardManagement（删除板块/成员上下移/成员移除）与 AdminEditForm（图片/QR 上传）的最小接线：加 emit、替换 demo 处理器，视觉/文案/布局/交互顺序零变化。
3. 冻结 UI 缺能力（sortMode 控件、QR 配置、服务端搜索选择器、管理 loading）记录为缺口移交 T06，不新增 UI。

## 实施中修复的缺陷

- 公开首页曾发起管理列表请求（401）：`useAdminGroups` 增加 `isActive` 门控。
- `syncToUrl` 重复 status 参数互相覆盖：改为数组 query。
- 板块成员操作后 memberCount 不刷新：`useAdminBoards.syncMemberCount`。
- 管理列表 loading 期间首次请求在非管理视图空跑。
- seed 脚本与既有 lint 基线错误（T04 已修）；本任务新增文件无 lint 错误。

## Mock 清理证据

- 生产 bundle 无 demo 数据（dist 扫描 0 命中）。
- `src/data/fixtures.ts` 仅保留类型与状态标签映射供冻结组件使用。
