# 群聊发现导航平台前端大版本升级——总 PRD

“来个群号” 网站 v2 大版本升级的总体产品需求文档（PRD）。

## 0. 文档信息

| 项目     | 内容                                                                                      |
| -------- | ----------------------------------------------------------------------------------------- |
| 总任务   | `lgqh-v2`                                                                                 |
| 文档类型 | 总体产品需求文档                                                                          |
| 目标版本 | 前端与管理端体验重大升级                                                                  |
| 技术栈   | Vue 3、Vite、TypeScript strict、Tailwind CSS 3、Hono 4、Cloudflare D1、Cloudflare R2、Zod |
| 测试体系 | Vitest、Workers Vitest、Playwright                                                        |
| 文档附属 | 10 份子任务 PRD                                                                           |

---

# 1. 项目背景

当前网站的公开端、管理端、后端 API、数据库、图片存储、安全机制和自动化测试基础均已成型。

现有系统已经具备：

- 公开群组浏览、搜索、提交和匿名点赞
- 群组卡片、无限滚动、URL 搜索同步
- 管理端群组 CRUD、筛选、排序、回收站
- PNG 图片上传和资源生命周期管理
- HMAC 会话、CSRF 防护和版本冲突处理
- Cloudflare D1、R2 和 Analytics 集成
- 前后端共享 Zod Schema
- Workers 集成测试和 Playwright E2E 测试

本次升级不是从零开发，而是在不破坏现有能力的基础上，对以下内容进行系统性重构：

1. 公开首页信息架构
2. 群组卡片与详情交互
3. 自定义板块功能
4. 管理端信息架构
5. 管理端分页和响应式表格
6. 全站设计语言
7. 深色模式
8. 响应式体验
9. 自动化测试和系统级回归

---

# 2. 项目目标

## 2.1 核心目标

### 目标一：提高首页的信息组织能力

首页不再只是一个单一群组网格，而是由发现、标签、自定义板块和完整群组目录共同组成。

### 目标二：降低卡片操作密度

群组卡片只承担快速浏览和点赞功能，完整群组信息及加群方式统一进入详情弹窗。

### 目标三：建立可配置内容板块

管理员可以创建多个板块，并控制板块状态、顺序、成员和排序模式。

### 目标四：建立统一设计系统

以用户后续提供的视觉模板为参考，形成统一的颜色、间距、圆角、字体、组件和响应式规范。

### 目标五：提升跨设备体验

公开端和管理端必须在桌面、平板、手机和常见系统主题下正常使用，不得因屏幕变窄而丢失核心功能。

### 目标六：保持现有系统稳定

本次升级不得破坏现有认证、提交、点赞、图片上传、回收站、版本冲突和资源清理机制。

---

# 3. 非目标

以下内容不属于本次升级范围：

- 不开发原生移动应用
- 不增加用户注册和普通用户账号系统
- 不将群组详情改为独立页面
- 不引入 WebSocket 或服务端实时推送
- 不重写现有管理员认证体系
- 不改变现有 R2 图片资源生命周期
- 不重新设计 Cloudflare Analytics 数据来源
- 不为板块成员数量设置硬性上限
- 不为管理端提供可切换的每页条数
- 不在第一版本实现板块成员拖拽排序
- 不在第一版本建立新的搜索相关性算法

---

# 4. 产品设计原则

## 4.1 发现与检索分离

有限、精选、可配置的内容使用横向 Carousel。

完整目录和搜索结果使用纵向 Grid。

| 区域       | 布局         |
| ---------- | ------------ |
| 发现新群   | Carousel     |
| 自定义板块 | Carousel     |
| 所有标签   | 标签聚合卡片 |
| 所有群组   | Grid         |
| 搜索结果   | Grid         |

## 4.2 摘要与详情分离

卡片负责摘要浏览，详情弹窗负责完整信息和加群操作。

## 4.3 状态语义明确

已发布、已下架、回收站和永久删除必须有不同的公开展示与关联处理规则。

## 4.4 设计系统先于页面实现

不得先完成全部页面，再在最后统一改视觉样式。

视觉模板必须在基础组件正式实现前完成设计语言提炼。

## 4.5 前后端共享约束

字符宽度、字段长度、板块排序模式和 API 数据结构必须通过共享类型或共享 Zod Schema 保持一致。

---

# 5. 公开端总体信息架构

## 5.1 默认首页

搜索框为空时，首页从上到下依次展示：

1. 顶栏
2. 主搜索框
3. 发现新群
4. 所有标签
5. 零个、一个或多个自定义板块
6. 所有群组

已下架群组更改为不在公开端出现。

## 5.2 搜索状态

搜索框存在有效内容时，页面自动切换为：

1. 顶栏
2. 位置不变的主搜索框
3. 搜索结果

以下默认首页区域暂时隐藏：

- 发现新群
- 所有标签
- 自定义板块
- 所有群组

清空搜索内容后，恢复默认首页。

## 5.3 搜索框位置稳定

搜索状态切换前后，搜索框的纵向位置和主要尺寸不得明显变化。

页面不能因为进入搜索模式而产生大幅布局跳动。

---

# 6. 顶栏

## 6.1 桌面布局

左侧：

- 网站 Logo
- 网站名称

右侧：

- 主题模式控件
- GitHub 支持按钮
- 添加新群组按钮

宽屏按钮使用“图标 + 文字”。

推荐文字：

- （太阳月亮图标）（根据当前设计方案显示“自动”“浅色”或“深色”）
- （GitHub Logo） ”支持项目”
- （加号图标） “添加新的群组”

## 6.2 窄屏布局

屏幕宽度不足时：

- Logo 和网站名称仍需保留
- 主题模式控件只显示图标
- GitHub 支持按钮只显示图标
- 添加按钮缩短为“添加群”三个字
- 添加按钮不得缩减为单独的加号

所有图标按钮必须具备：

- 可访问名称
- Tooltip 或等效提示
- 清晰的键盘焦点状态

## 6.3 顶栏响应式原则

- 不打乱左右区域语义
- 不隐藏核心功能
- 优先缩减间距、文字和按钮装饰
- 不使用移动端汉堡菜单替代当前三个主要操作
- 不产生页面级横向滚动

---

# 7. 深色模式

## 7.1 主题偏好

系统支持三种偏好：

```ts
type ThemePreference = "system" | "light" | "dark";
```

默认值为：

```ts
"system";
```

## 7.2 优先级

1. 浏览器本地存在合法用户偏好时，使用该偏好
2. 不存在本地偏好时，使用 `system`
3. `system` 模式根据 `prefers-color-scheme` 决定实际主题

## 7.3 自动模式

自动模式必须：

- 支持 iOS、macOS、Windows、Android 等系统主题检测
- 在系统主题发生变化时实时跟随
- 不只在首次进入网站时检测一次

## 7.4 本地持久化

用户选择的模式保存在 localStorage。

本地值损坏或不合法时，自动回退到 `system`。

## 7.5 首屏主题

主题必须在 Vue 应用挂载前尽早应用，避免出现先亮后暗或先暗后亮的闪烁。

## 7.6 覆盖范围

深色模式必须覆盖：

- 公开首页
- 搜索结果
- 群组详情弹窗
- 提交群组页面或弹窗
- 登录页
- 管理端群组管理
- 管理端板块管理
- 数据分析页
- 管理抽屉
- Toast
- ErrorBanner
- Skeleton
- 空状态
- 二维码弹窗
- 表单和表格

---

# 8. 统一设计语言

## 8.1 视觉模板输入

用户将在后续提供一套视觉模板。

Agent 不得直接复制模板代码并立即全站替换，而应先提取其设计语言。

## 8.2 设计规范交付物

需要建立：

```text
ui-design.md
```

至少包含：

### 基础 Token

- 主色
- 辅助色
- 页面背景
- 卡片背景
- 分层文字颜色
- 边框颜色
- 圆角等级
- 阴影等级
- 间距体系
- 字号和行高
- 字重
- 动画时长
- 动画缓动
- 浅色与深色映射

### 组件规范

- 顶栏
- 搜索框
- 群组卡片
- 群组详情弹窗
- Carousel
- Grid
- 标签
- 主按钮
- 次按钮
- 图标按钮
- 表格
- 管理端板块容器
- 表单
- 抽屉
- Toast
- ErrorBanner
- Skeleton
- 空状态
- 分页器

### 响应式规范

- 页面最大宽度
- 页面边距
- 卡片宽度
- Grid 列数
- Carousel 间距
- 管理端断点
- 弹窗和抽屉宽度
- 顶栏压缩规则

## 8.3 视觉验证关卡

在全站实施前，先制作小范围视觉样例：

- 顶栏
- 搜索框
- 一张群组卡片
- 一段 Carousel
- 群组详情弹窗
- 一行管理表格
- 浅色模式
- 深色模式

该样例经用户确认后，其他子任务才能正式采用设计系统。

---

# 9. 群组内容长度规则

## 9.1 显示宽度计算

不能直接使用 JavaScript `string.length` 作为限制依据。

需要建立前后端共享的显示宽度计算规则。

建议共享函数：

```ts
measureDisplayWidth(value: string): number
```

基本规则：

- ASCII 字符：1 个宽度单位
- 半角拉丁字符：1 个宽度单位
- 中文、日文、韩文和全角字符：2 个宽度单位
- 单个 Emoji 字素簇：2 个宽度单位
- 组合 Emoji 按一个可见字素计算
- 带重音的拉丁字母不能简单按非 ASCII 统一计算为 2
- 前后空格在校验前清理
- 换行符计入合理宽度，防止通过大量换行绕过限制

具体 Unicode 算法由技术设计确定，但前后端结果必须完全一致。

## 9.2 标题限制

群组标题最大：

```text
50 个显示宽度单位
```

标题在卡片上最多显示两行。

极端情况下允许视觉省略，但原始标题完整保存在数据中，并在详情弹窗中显示。

## 9.3 简介限制

群组简介最大：

```text
1000 个显示宽度单位
```

简介不允许无限制存储。

卡片中最多显示四行，超出部分使用视觉省略号。

详情弹窗中显示完整简介。

## 9.4 表单反馈

创建和编辑表单应显示：

- 当前已使用宽度单位
- 最大宽度单位
- 超限错误

客户端和服务端必须使用同一个共享校验规则。

---

# 10. 群组卡片

## 10.1 卡片展示内容

所有公开区域统一使用同一种群组卡片。

卡片展示：

- 群组头像
- 群组标题
- 平台
- 最多四行简介摘要
- 点赞按钮
- 点赞数量

卡片不直接展示：

- 群号
- 加群链接
- 二维码
- 加群方式按钮
- 复制按钮

## 10.2 卡片点击

点击卡片主体后打开群组详情弹窗，不跳转页面。

以下操作不得触发打开弹窗：

- 点击点赞按钮
- 在 Carousel 中完成拖动
- 点击卡片中的其他独立交互控件

## 10.3 可访问性

卡片必须支持：

- 鼠标点击
- 触摸点击
- 键盘聚焦
- Enter 打开
- Space 打开

不得通过嵌套原生按钮制造非法交互结构。

点赞按钮必须保持独立可聚焦和可操作。

---

# 11. 群组详情弹窗

## 11.1 弹窗内容

详情弹窗展示：

- 群组头像
- 完整标题
- 平台
- 完整简介
- 点赞按钮
- 点赞数量
- 一个或多个加群方式
- 二维码、复制、打开链接等现有能力
- 分享按钮

多个加群方式按照管理员配置顺序展示。

## 11.2 URL 状态

打开详情弹窗时，URL 增加：

```text
?group=<group_id>
```

当页面同时存在搜索参数时：

```text
/?q=计算机&group=<group_id>
```

关闭弹窗后只移除 `group` 参数，保留其他查询参数。

## 11.3 深链接

直接访问：

```text
/?group=<group_id>
```

时，页面加载完成后自动打开对应群组详情弹窗。

公开详情接口只能返回已发布群组。

无效、已下架、回收站或已删除的群组不得通过深链接泄露信息。

## 11.4 分享按钮

点击分享按钮后，复制群组的规范化公开链接：

```text
https://当前域名/?group=<group_id>
```

复制的分享链接默认不携带搜索词等无关查询参数。

复制成功后显示 Toast。

复制失败时提供明确错误反馈。

## 11.5 浏览器历史

- 从卡片打开弹窗后，浏览器返回操作可以关闭弹窗
- 浏览器前进可以重新打开弹窗
- 关闭弹窗不得清除搜索词和页面筛选状态
- 关闭后焦点返回原卡片

## 11.6 弹窗交互

必须支持：

- Escape 关闭
- 点击遮罩关闭
- 明确的关闭按钮
- 焦点锁定
- 关闭后焦点恢复
- 打开后锁定底层滚动
- 手机端接近全屏
- 长简介在弹窗内容区域内滚动
- 深色模式
- 减少动态效果偏好

---

# 12. Carousel

## 12.1 使用区域

Carousel 只用于：

- 发现新群
- 自定义板块

所有群组和搜索结果不得使用超长单行 Carousel。

## 12.2 响应式卡片数量

Carousel 中的卡片数量根据容器宽度调整。

在标准手机宽度下，视口内至少能够同时看到两张卡片。

不得因为屏幕变窄而改成单卡独占整行。

## 12.3 交互方式

支持：

- 手指横向滑动
- 鼠标按住拖动
- 触控板横向滚动
- 鼠标滚轮横向查看
- 键盘左右方向键
- `scroll-snap`

## 12.4 鼠标滚轮规则

只有在 Carousel 存在横向溢出且仍可继续横向滚动时，才将滚轮操作用于横向移动。

滚动到左右边界后，应允许页面继续纵向滚动，不能锁死页面。

## 12.5 点击与拖动冲突

拖动距离超过阈值后：

- 本次动作视为拖动
- 不触发卡片点击
- 不打开详情弹窗

## 12.6 溢出提示

设计语言可采用以下一种或多种方式提示内容可横向查看：

- 部分露出下一张卡片
- 左右渐变遮罩
- 桌面端左右箭头
- 滚动位置指示

具体视觉方案由 `ui-design.md` 确定。

---

# 13. 发现新群

## 13.1 定义

“发现新群”展示最近一次进入已发布状态的群组。

不再使用随机排序。

## 13.2 数据字段

群组表新增：

```text
last_published_at
```

该字段允许为空。

## 13.3 更新时间规则

仅在群组发生以下状态转换时更新：

```text
非已发布状态 → 已发布状态
```

具体包括：

- 新建后直接发布
- 草稿或待审核状态发布
- 已下架群组重新发布
- 从回收站恢复后再次发布

以下操作不更新时间：

- 编辑已发布群组的标题
- 编辑简介、标签、头像和加群方式
- 保存一个仍然保持已发布状态的群组

## 13.4 排序

查询规则：

```text
status = published
ORDER BY last_published_at DESC, id DESC
LIMIT 10
```

`id` 作为稳定次排序字段。

## 13.5 更新方式

发现新群不需要 WebSocket 或自动轮询。

用户重新进入、刷新页面或触发重新请求时，即可看到最新发布群组。

## 13.6 数量

- 默认最多展示 10 个群组
- 少于 10 个时展示全部
- 不支持无限加载
- 使用 Carousel 展示

---

# 14. 所有标签

## 14.1 统计范围

只统计已发布群组。

已下架、回收站和永久删除群组不计入数量。

## 14.2 标签卡片

“所有标签”使用一个独立大卡片展示：

- 标签名称
- 对应已发布群组数量

## 14.3 点击行为

点击标签后：

1. 用标签文本替换搜索框当前内容
2. 更新 URL 中的搜索参数
3. 进入搜索结果状态
4. 不跳转到新页面

标签不会追加到已有搜索词后面。

## 14.4 排序

默认建议：

1. 群组数量降序
2. 标签名称作为稳定次排序

最终排序规则可在标签子任务 PRD 中确认。

## 14.5 聚合查询

标签数量必须通过聚合查询获得，不得为每个标签单独发起数据库查询。

---

# 15. 自定义板块

## 15.1 板块能力

管理员可以：

- 创建板块
- 修改标题
- 启用板块
- 关闭板块
- 删除板块
- 拖拽调整板块之间的顺序
- 修改板块内部排序模式
- 添加群组
- 移除群组
- 上移群组
- 下移群组

## 15.2 板块公开规则

只有启用状态的板块出现在公开首页。

未启用板块：

- 不在公开端显示
- 仍可在管理端正常编辑
- 保留所有成员和排序配置

## 15.3 空板块

启用但没有可显示成员的板块，在公开端展示空状态。

板块数量为零时，首页不显示自定义板块区域。

## 15.4 默认板块

首次初始化或迁移数据库时，自动创建一个空板块：

```text
自定板块
```

该板块：

- 不具有特殊系统权限
- 可以编辑
- 可以关闭
- 可以删除
- 删除后允许板块总数为零

## 15.5 板块成员

- 一个群组可以加入多个不同板块
- 同一群组不能在同一板块中重复
- 板块成员数量暂不设置硬上限
- 管理端可以对过长板块显示软性体验提示，但不得阻止保存

## 15.6 群组状态与关联

| 群组状态 | 板块关联   | 公开端 |
| -------- | ---------- | ------ |
| 已发布   | 保留       | 显示   |
| 已下架   | 保留       | 不显示 |
| 回收站   | 移除       | 不显示 |
| 永久删除 | 不存在关联 | 不显示 |

管理员允许向板块添加：

- 已发布群组
- 已下架群组

不得添加已经进入回收站的群组。

## 15.7 回收站行为

将群组移入回收站时：

- 删除该群组的全部板块关联
- 群组状态变化和关联删除必须原子完成
- 应复用现有突变令牌和批量操作模式

从回收站恢复后：

- 不自动恢复原板块关联
- 管理端应明确提示该行为

---

# 16. 板块排序

## 16.1 排序模式

```ts
type BoardSortMode = "manual_asc" | "manual_desc" | "hourly_random";
```

## 16.2 手动正序

按照管理员配置的成员位置正序展示。

## 16.3 手动倒序

按照管理员配置的成员位置倒序展示。

管理端仍按照标准位置顺序编辑，公开端负责反向展示。

## 16.4 每小时随机

随机模式必须满足：

- 同一个自然小时内顺序稳定
- 刷新页面不会改变顺序
- 下一个小时自动形成新顺序
- 不通过定时任务修改数据库位置
- 不实际覆盖人工排序位置

建议使用：

```text
board_id + 小时槽位 + group_id
```

计算稳定随机顺序。

小时槽位应使用站点配置时区，而不是依赖服务器本地时区。

---

# 17. 所有群组

## 17.1 布局

使用响应式 Grid。

不得使用单行超长 Carousel。

## 17.2 数据范围

只展示已发布群组。

## 17.3 排序

继续沿用当前系统默认排序机制，包括现有时区感知旋转规则。

本次升级不重新定义完整目录的排序算法。

## 17.4 分页

继续使用 cursor pagination 和无限滚动。

产品层面允许用户最终浏览全部群组，但不得一次性从 API 返回全部记录。

## 17.5 响应式

在标准手机宽度下，目标为至少两列卡片。

极端窄屏和高倍文字缩放场景下，应优先保证内容可用性和无横向溢出。

---

# 18. 搜索结果

## 18.1 搜索触发

- 无搜索按钮
- 不需要按 Enter
- 输入后经过 debounce 自动搜索
- 中文输入法组合阶段不得反复发送无效请求

## 18.2 URL 同步

搜索词保存在：

```text
?q=<keyword>
```

刷新页面、浏览器返回和前进操作必须恢复搜索状态。

## 18.3 搜索范围

继续搜索：

- 标题
- 简介
- 标签

本次升级不改变现有搜索匹配语义和排序算法。

## 18.4 分页

搜索结果继续使用 cursor pagination。

## 18.5 状态

必须定义并展示：

- 初始状态
- debounce 等待状态
- 加载状态
- 已有结果继续加载状态
- 无结果状态
- 请求失败状态
- 重试状态

搜索请求期间不得造成搜索框位置移动。

---

# 19. 公开端加载、空状态和错误状态

## 19.1 发现新群

无数据时显示明确空状态。

## 19.2 所有标签

没有有效标签时显示空状态。

## 19.3 自定义板块

- 启用且无公开成员：显示板块空状态
- 板块总数为零：不显示该区域
- 单个板块加载失败：显示该板块错误状态，不使整个首页白屏

## 19.4 所有群组

没有已发布群组时显示空状态，并可引导用户提交群组。

## 19.5 搜索结果

无结果时：

- 显示搜索关键词
- 提示没有匹配群组
- 提供提交新群组入口

## 19.6 群组详情深链接

群组不存在或不可公开时：

- 不返回下架群组详情
- 显示非敏感错误反馈
- 允许用户关闭或清理无效 `group` 参数

---

# 20. 管理端信息架构

管理端主要导航调整为：

1. 群组管理
2. 板块管理
3. 数据分析

现有认证、登录、会话和权限机制保持不变。

---

# 21. 管理端群组管理

## 21.1 页码分页

群组管理页面由 keyset pagination 改为页码分页。

固定每页：

```text
50 条
```

第一版本不提供每页数量切换。

## 21.2 返回结构

管理端列表 API 返回：

```ts
{
  items: Group[]
  page: number
  pageSize: 50
  totalItems: number
  totalPages: number
}
```

## 21.3 URL 状态

以下状态必须同步至 URL：

- 页码
- 搜索词
- 状态筛选
- 回收站筛选
- 排序字段
- 排序方向

示例：

```text
/admin/groups?page=3&status=published&sort=updated_at&direction=desc
```

## 21.4 状态变化规则

- 修改筛选时回到第一页
- 修改排序时回到第一页
- 修改搜索词时回到第一页
- 删除当前页最后一项并导致页面为空时，自动回到上一页
- 刷新后恢复当前状态
- 浏览器返回和前进恢复当前状态

## 21.5 分页器

分页器采用：

```text
上一页  1  …  5  6  [7]  8  9  …  24  下一页
```

同时展示：

```text
共 1,173 条，第 7 / 24 页
```

## 21.6 稳定排序

所有排序必须添加稳定次排序字段，避免相同值记录在不同页面间跳动。

## 21.7 保留现有能力

以下现有功能继续保留：

- 多状态筛选
- 回收站
- 多列排序
- 抽屉编辑
- 新建群组
- 脏表单守卫
- 版本冲突处理
- 图片上传
- 标签编辑
- 加群方式编辑
- 永久删除流程

---

# 22. 管理端响应式表格

## 22.1 永久保留列

以下列永不隐藏：

- 标题
- 状态
- 操作

## 22.2 隐藏顺序

屏幕或容器宽度不足时，按以下顺序依次隐藏：

1. 标签
2. 性质
3. 点赞
4. 平台

## 22.3 规则含义

产品规则是列优先级，具体 CSS 断点由技术设计和视觉规范确定。

## 22.4 隐藏信息访问

即使某列隐藏，管理员仍可通过编辑抽屉查看完整字段。

## 22.5 抽屉

窄屏下：

- 编辑和新建抽屉占满页面
- 锁定底层滚动
- 保留现有 Escape、beforeunload 和路由守卫
- 操作按钮不得因安全区或软键盘而不可访问

---

# 23. 管理端板块管理

## 23.1 页面结构

板块管理页面由多个板块容器组成。

每个板块容器包括：

- 拖拽手柄
- 板块标题
- 启用或关闭状态
- 排序模式
- 当前成员数量
- 添加群组
- 编辑板块
- 删除板块
- 固定尺寸成员表格

## 23.2 板块拖拽

管理员可以拖拽调整板块之间的展示顺序。

拖拽完成后：

- 批量更新 `boards.position`
- 使用原子操作
- 复用版本冲突或等效并发保护
- 失败时回滚前端乐观排序并提示错误

## 23.3 成员表格

每个板块有一个独立小表格。

表格固定可视高度，超出后在表格内部滚动，不使用页码分页。

固定表格列：

| 列   | 内容                   |
| ---- | ---------------------- |
| 标题 | 群组标题               |
| 状态 | 已发布或已下架         |
| 操作 | 编辑、上移、下移、移除 |

最终像素高度和可见行数由 `ui-design.md` 确定。

## 23.4 成员操作

### 编辑

打开现有群组编辑抽屉。

### 上移

将群组与前一个成员交换位置。

第一项禁用上移。

### 下移

将群组与后一个成员交换位置。

最后一项禁用下移。

### 移除

只删除板块和群组之间的关联。

不得将该按钮命名为“删除群组”。

## 23.5 添加群组

添加选择器必须：

- 支持搜索
- 展示已发布群组
- 展示已下架群组
- 不展示回收站群组
- 清晰显示当前状态
- 对已加入当前板块的群组禁用或隐藏
- 防止重复添加

## 23.6 删除板块

删除板块需要二次确认。

删除后：

- 板块成员关联一并删除
- 群组本身不受影响
- 允许删除最后一个板块

## 23.7 未启用板块

未启用板块在管理端仍允许：

- 重命名
- 调整成员
- 调整排序
- 修改排序模式
- 删除

#

---

# 24. 数据库设计

## 24.1 群组表新增字段

```text
last_published_at
```

建议类型与现有时间字段一致，可为空。

需要建立适合发现新群查询的索引。

## 24.2 板块表

```text
boards
- id
- title
- is_enabled
- position
- sort_mode
- version
- created_at
- updated_at
```

字段含义：

- `position`：板块在首页中的顺序
- `sort_mode`：板块内部展示方式
- `version`：并发修改保护

## 24.3 板块成员表

```text
board_groups
- board_id
- group_id
- position
- created_at
```

## 24.4 约束

```text
PRIMARY KEY (board_id, group_id)
```

还需具备：

- 板块删除时级联删除成员关联
- 群组永久删除时清除成员关联
- `board_id + position` 查询索引
- 板块位置查询索引
- 外键与当前 D1 配置兼容

## 24.5 位置维护

成员上移、下移和板块拖拽必须使用原子批量更新。

不得留下重复位置、丢失位置或部分更新状态。

---

# 25. 数据迁移

## 25.1 `last_published_at` 回填

当前网站尚未上架，没有需要兼容的旧内容。迁移后现有群组的 `last_published_at` 全部保持 `NULL`：

- 不使用 `created_at`、`updated_at`、当前 `status` 或迁移执行时间推断发布时间
- 不执行历史发布时间回填
- 未来只有真实的非 `published` → `published` 成功转换才由服务端时钟写入

如果未来网站已上线后需要补录历史发布时间，必须另立可审计的数据迁移决策，不能在本次 V2 迁移中静默引入兜底规则。

## 25.2 默认板块

迁移或新数据库初始化时创建一个空的“自定板块”。

该操作必须幂等：

- 重复运行迁移不能创建多个默认板块
- 已存在板块数据时不得重复插入

## 25.3 回滚和兼容

迁移执行前需要确认：

- 旧代码在短暂部署窗口内是否能容忍新增字段和表
- 新代码在迁移尚未完成时如何失败
- 本地测试数据库和 Workers 测试数据库同步更新

---

# 26. API 与共享 Contract

## 26.1 共享类型

需要新增或更新：

- `ThemePreference`
- `BoardSortMode`
- `Board`
- `BoardMember`
- `BoardWithGroups`
- 管理端页码分页结构
- 公开端群组详情结构
- 字符显示宽度校验
- `last_published_at`

## 26.2 公开接口

至少覆盖：

- 发现新群
- 所有标签统计
- 启用板块和已发布成员
- 所有群组 cursor pagination
- 搜索结果 cursor pagination
- 已发布群组详情

具体是聚合首页接口还是多个独立接口，由 `design.md` 确定。

## 26.3 管理接口

至少覆盖：

- 板块列表
- 创建板块
- 编辑板块
- 启用或关闭
- 删除板块
- 调整板块顺序
- 添加板块成员
- 移除板块成员
- 上移或下移成员
- 修改排序模式
- 管理端群组页码列表

## 26.4 安全要求

所有管理端写操作继续使用：

- 管理员认证
- CSRF 保护
- Zod 校验
- 版本冲突保护
- 突变令牌或现有原子批量模式

公开接口不得返回下架、回收站或永久删除群组的敏感内容。

---

# 27. 性能要求

## 27.1 数据加载

- 所有群组和搜索结果保持 cursor pagination
- 管理端每页固定 50 条
- 发现新群最多 10 条
- 标签通过单次聚合查询
- 板块成员接口不得产生明显 N+1 查询
- 图片继续懒加载
- 图片保持固定比例，避免布局跳动

## 27.2 DOM 规模

板块成员管理表使用固定高度和内部滚动，避免同时渲染过长页面。

公开 Carousel 虽不设产品硬上限，但实现需要避免明显主线程阻塞。

## 27.3 主题性能

首屏主题设置不能依赖 Vue 挂载完成后才执行。

## 27.4 稳定随机

每小时随机排序必须可计算、可缓存、可测试，不运行每小时写数据库的定时任务。

---

# 28. 无障碍要求

至少满足：

- 所有操作可通过键盘完成
- 焦点状态明显
- 弹窗焦点锁定和恢复
- Carousel 支持键盘左右移动
- 图标按钮存在可访问名称
- 颜色不能作为唯一状态表达
- 深色和浅色模式均有合理对比度
- 触摸目标尺寸合理
- 支持 `prefers-reduced-motion`
- Toast 和错误提示可以被辅助技术感知
- 表格保持正确表头和单元格语义

---

# 29. 测试策略

所有新增和修改功能必须有自动化测试。

前端关键用户流程必须通过 Playwright 验证。

## 29.1 Vitest

覆盖：

- 字符显示宽度计算
- ASCII、中文、全角字符
- 单个和组合 Emoji
- 标题 50 宽度边界
- 简介 1000 宽度边界
- 主题偏好解析
- 非法本地主题值回退
- 系统主题变化
- 小时槽位计算
- 稳定随机排序
- URL 查询参数处理
- 分页页码生成

## 29.2 Workers Vitest

覆盖：

- `last_published_at` 状态转换
- 已发布编辑不更新时间
- 下架重新发布更新时间
- 发现新群最近十条排序
- 相同时间戳稳定排序
- 标签只统计已发布群组
- 板块 CRUD
- 板块启用和关闭
- 板块拖拽位置更新
- 成员上移和下移
- 添加已下架群组
- 防止重复成员
- 回收站原子移除关联
- 永久删除关联清理
- 管理端页码总数
- 第 50、51 条边界
- 排序跨页稳定
- 权限和 CSRF

## 29.3 Playwright：公开端

覆盖：

1. 默认首页区域顺序
2. 搜索框输入后自动进入搜索结果
3. 清空搜索恢复默认首页
4. 搜索框位置不跳动
5. 标签点击替换搜索词
6. 所有群组 Grid 无限滚动
7. 搜索结果 cursor pagination
8. 卡片最多四行简介
9. 点击卡片打开详情弹窗
10. 点击点赞不打开弹窗
11. Carousel 拖动不误开弹窗
12. `?group=` 直接打开详情
13. 浏览器返回关闭弹窗
14. 分享按钮复制规范链接
15. 多个加群方式顺序正确
16. 下架群组无法公开访问
17. 手机端至少显示两张 Carousel 卡片
18. 鼠标拖动、滚轮和触摸滑动
19. 深色模式三态切换
20. 刷新后主题偏好保留
21. 自动模式跟随系统变化

## 29.4 Playwright：管理端

覆盖：

1. 每页固定 50 条
2. 正确的总页数
3. URL 恢复页码和筛选
4. 筛选后回到第一页
5. 删除最后一条后返回上一页
6. 响应式列按指定顺序隐藏
7. 标题、状态和操作永不隐藏
8. 窄屏抽屉全屏
9. 创建板块
10. 编辑和删除板块
11. 启用和关闭板块
12. 未启用板块仍可编辑
13. 拖拽调整板块顺序
14. 添加已发布群组
15. 添加已下架群组
16. 上移和下移成员
17. 移除成员不删除群组
18. 固定高度表格内部滚动
19. 板块数量为零
20. 回收站移除关联
21. 版本冲突提示

## 29.5 视觉回归

设计语言通过确认后，为关键页面建立截图基线：

- 首页桌面浅色
- 首页桌面深色
- 首页手机浅色
- 首页手机深色
- 群组详情弹窗
- 管理端群组列表
- 板块管理页面
- 管理抽屉

测试时间需要固定，避免小时随机和时间字段造成截图不稳定。

---

# 30. 全局验收标准

本次升级只有同时满足以下条件才能验收：

1. 所有 Spec 已完成审计
2. 子任务 PRD、设计和实施文档齐全
3. 视觉设计语言经过用户确认
4. 公开首页结构符合本 PRD
5. 已下架群组不在公开端泄露
6. 群组卡片和详情弹窗行为完整
7. 深链接和分享链接正常
8. 三态主题正常并持久化
9. 板块 CRUD 和排序完整
10. 回收站关联清理原子完成
11. 管理端页码分页每页 50 条
12. 管理表格响应式列顺序正确
13. 公开端 cursor pagination 保持可用
14. 现有提交、点赞、上传和删除能力无回归
15. Vitest、Workers Vitest 和 Playwright 全部通过
16. 浅色、深色、桌面和手机关键页面通过视觉验收
17. 没有页面级横向溢出
18. 没有严重键盘和焦点问题
19. 数据迁移可在现有数据库上安全执行
20. 所有新增管理操作符合现有安全模式

---

# 31. Trellis 总任务结构

## 31.1 总任务

```text
lgqh-v2
```

总任务定位为：

> 规划、拆分、协调和最终验收任务。

总任务原则上不直接承担大块功能开发。

## 31.2 总任务职责

总任务负责：

1. 阅读本总 PRD
2. 阅读全部现有 Spec
3. 建立所有子任务目录和基础文件
4. 为子任务写明范围、依赖和验收要求
5. 维护任务依赖图
6. 防止不同子任务重复修改同一领域
7. 记录跨任务决策
8. 跟踪迁移顺序
9. 汇总测试矩阵
10. 在全部子任务完成后进行系统级验收
11. 运行完整测试
12. 检查遗漏和回归
13. 只处理必要的集成问题，不在总任务中重新开发子功能

## 31.3 总任务交付物

```text
prd.md
design.md
implement.md
research.md
acceptance.md
```

其中：

- `prd.md`：本总体产品规划
- `research.md`：代码和 Spec 审计结果
- `design.md`：跨任务架构和依赖设计
- `implement.md`：任务执行顺序和集成方式
- `acceptance.md`：最终系统验收清单

---

# 32. Trellis 子任务划分（重规划版）

本章重写后只定义当前有效的 T01-T06 任务体系。原有 T04-T10 拆分已经废弃，旧子任务 PRD 不再作为产品边界、实施依据或验收依据。历史 Trellis 任务可以在归档目录中查阅，但不得重新激活。

## 32.1 权威来源与任务状态

本章唯一的产品依据是：

```text
docs/PRD/v2/PRD.md
```

`docs/PRD/v2/PRD.md` 是当前人工审核的总 PRD。docs/PRD/v2 目录下原有的子任务 PRD 全部过时并删除，不再保留第二套子任务边界。

当前任务状态如下：

| 任务 | 状态                  | 说明                                                          |
| ---- | --------------------- | ------------------------------------------------------------- |
| T01  | 已完成                | 基础工程、项目框架、既有后端能力和现状审计已完成              |
| T02  | 已完成                | prototype v2 视觉标准和完整前端设计已完成                     |
| T03  | 进行中/由独立任务维护 | prototype v2 向正式项目迁移及已有业务接入，不得被后续任务打扰 |
| T04  | planning              | 后端能力扩展                                                  |
| T05  | planning              | 全栈业务适配                                                  |
| T06  | planning              | 系统加固与发布验收                                            |

T04-T06 的规划文件位于 .trellis/tasks 下，并且每个任务包含 prd.md、design.md 和 implement.md。T04-T06 在用户明确批准前不得自动进入实施。

## 32.2 T01-T03 已完成基线与边界

### T01：基础工程与既有后端能力

T01 已完成以下基础：

- Vue 3、Vite、TypeScript、Tailwind、Hono、D1、R2、Zod 及测试体系。
- 公开浏览、搜索、提交、匿名点赞等已有业务能力。
- 管理端认证、群组 CRUD、筛选、排序、回收站和永久删除。
- HMAC 会话、CSRF、版本冲突、突变令牌和既有原子批量模式。
- PNG 图片上传和 R2 资源生命周期。
- Workers Vitest、Vitest 和 Playwright 基础。

T04 只能在这些能力上扩展，不得没有证据地重写认证、存储或已有状态机。

### T02：prototype v2 视觉真源

T02 已完成完整前端设计。prototype v2 是唯一的产品视觉真源，包含：

- 页面结构和信息架构。
- Vue 页面和表现组件。
- CSS、设计 Token、颜色、间距、圆角、阴影和字体。
- Dialog、Card、Carousel、Table、Drawer、Form、Toast、Error 和 Skeleton。
- 响应式布局、主题、动画和交互流程。
- 公开端及管理端关键页面。

T04-T06 不得重新设计、重排、替换或“优化”这些内容。

### T03：正式项目迁移与已有业务接入

T03 负责将 prototype v2 迁移到正式项目并接入已有业务能力。独立任务 t03-visual-migration 仍在进行中，T04-T06 必须遵守以下规则：

- 不修改、不归档、不重启、不切换 t03-visual-migration。
- 不修改其任务目录、分支、上下文文件或工作区。
- 不覆盖其正在处理的正式项目文件。
- 不把 T03 的进行中状态当作后续任务可以接管的工作。
- 发现 T03 相关问题时只记录并与用户协调。
- T04-T06 的后端和适配工作必须尊重 T03 最终交付的视觉基线。

prototype v2 不作为生产运行时依赖。正式项目可以复用已确认的视觉语义，但生产路径不能加载 prototype fixture、临时 Mock 或未经批准的 localStorage 业务数据。

## 32.3 前端冻结规则

从 T03 完成迁移并形成正式基线后，T04-T06 进入前端冻结范围。

### 禁止修改

T04、T05、T06 不得修改：

- 页面结构、区域顺序和信息架构。
- Vue 页面、表现组件和 Dialog 结构。
- CSS、Tailwind class、颜色、间距、布局、断点、圆角、阴影和字体。
- 动画、过渡、响应式表现和主题视觉。
- Card、Carousel、Table、Drawer、Form、Toast、Error、Skeleton 和空状态设计。
- 按钮布局、控件排列、文案层级和用户交互流程。
- prototype v2 及其视觉基线截图。

### 允许修改

在不改变表现层和交互语义的前提下，允许：

- 后端 service、repository、route、数据库和 migration。
- API 请求/响应、共享 Contract、Zod Schema 和数据结构。
- 管理权限、认证、CSRF、版本冲突和业务校验。
- 既有 R2 文件存储接口的必要服务端兼容。
- 前端 API client、composable、store、query adapter、DTO mapper 和请求生命周期。
- 测试、测试夹具、固定时间/时区/主题工具、构建、部署和发布文档。

如果真实数据或接口无法适配冻结前端，优先修正 Contract、后端或非表现层 adapter。确实需要表现层变化时，必须停止当前工作、记录问题、提出方案并与用户 brainstorm，未经批准不得直接修改。

## 32.4 T04：后端能力扩展

### 任务名称

```text
04 backend-feature-expansion
```

中文名称：后端能力扩展。

### 任务目标

依据本总 PRD，为已冻结的正式前端提供完整、稳定、可测试、可迁移和可回滚的后端能力，覆盖数据库、API、业务逻辑、权限、校验、文件存储兼容和管理端接口。

### 工作范围

#### 数据库和迁移

- 新增 last_published_at，并保持现有数据初始值为 NULL。
- 新增 boards 和 board_groups 及索引、外键、主键、级联和位置约束。
- 幂等创建“自定板块”默认数据。
- 支持重复迁移、旧代码兼容、迁移失败恢复和回滚演练。
- 同步本地数据库和 Workers 测试数据库。

#### 业务和查询

- 实现发布状态转换和 last_published_at 更新时间规则。
- 实现发现新群，按发布时间和 id 稳定排序，最多返回 10 条。
- 实现只统计 published 群组的标签聚合。
- 实现公开板块、公开成员、公开目录、搜索和已发布详情。
- 保持公开端 cursor pagination 和现有目录排序语义。
- 实现板块 CRUD、启停、顺序、排序模式、成员添加、移除、上移和下移。
- 实现 manual_asc、manual_desc、hourly_random 三种排序模式。
- 实现回收站原子移除板块关联，永久删除清理关联，恢复不自动重建关联。
- 实现管理端固定每页 50 条的页码分页、总数和稳定排序。

#### Contract、安全和存储

- 提供 Board、BoardMember、BoardWithGroups、BoardSortMode、公开详情和管理分页 Contract。
- 提供共享显示宽度规则、标题 50 单位和简介 1000 单位校验。
- 复用管理员认证、CSRF、Zod、版本冲突、突变令牌和原子批量模式。
- 公开接口不得返回下架、回收站或永久删除数据。
- 复用现有 R2 生命周期，补齐必要的服务端兼容，不改变既有清理规则。
- 错误响应必须区分校验、认证、权限、冲突、不可公开、存储和内部错误。

### 不包含内容

T04 不包含：

- 任何 Vue 页面、表现组件、CSS、Tailwind、主题、布局、动画或 Dialog 修改。
- 任何 prototype v2 设计修改。
- 任何用户交互流程重排。
- 生产前端 Mock 替换和真实 API 接线。
- T05 的 adapter/composable 业务适配。
- T06 的最终性能、安全、部署、迁移演练和发布验收。
- 新搜索算法、实时推送、普通用户账号或原生移动端。

### 依赖关系

T04 依赖 T01 的工程和后端基线、T02 的冻结视觉约束、T03 的正式项目迁移结果以及本总 PRD。T04 不等待 T05，可以在不修改前端的前提下独立完成。

T04 完成后必须向 T05 移交接口清单、请求/响应示例、错误码、权限/CSRF 要求、分页/排序/时区语义、迁移版本、测试结果、已知问题和回滚说明。

### 验收标准

- 数据库迁移可在空库和现有库执行，重复运行幂等，失败可恢复。
- 现有群组 last_published_at 全部保持 NULL。
- 只有非 published → published 成功转换才更新时间。
- 发现新群、标签、板块、详情、搜索和目录符合公开数据边界。
- 板块 CRUD、排序、成员操作、版本冲突和原子清理完整。
- 管理分页固定 50 条，totalItems、totalPages 和稳定排序正确。
- 显示宽度前后端一致，标题和简介边界有自动化测试。
- 认证、CSRF、权限、校验、R2 和既有业务回归通过。
- 未修改任何前端表现层；发现视觉问题时有记录而非直接修改。

## 32.5 T05：全栈业务适配

### 任务名称

```text
05 fullstack-integration
```

中文名称：全栈业务适配。

### 任务目标

在 T04 API 和 Contract 稳定后，将 T03 正式项目从 Mock/fixture 接入真实后端，完成公开端和管理端真实业务流程，同时保持 prototype v2 像素级一致。

### 工作范围

#### 真实数据接线

- 清理生产路径中的 prototype fixture、正式项目 Mock、静态假数据和业务 localStorage fallback。
- 接入真实 API client、composable、store、query adapter 和 DTO mapper。
- 接入首页、发现新群、标签、板块、目录、搜索、详情、深链、分享、点赞和提交。
- 接入管理登录、session、群组 CRUD、固定 50 条分页、筛选、排序、URL 状态、上传、回收站、永久删除和分析。
- 保留已有页面、Dialog、表单、表格、抽屉和导航流程。

#### 请求状态和错误

- 接入初始加载、debounce、loading、loading-more、空状态、区域错误、重试和取消请求。
- 接入认证失效、权限失败、CSRF 失败、校验失败、版本冲突、上传失败和网络超时。
- 防止过时搜索、详情和分页请求覆盖新结果。
- 不用假数据掩盖 API 失败，不通过前端权限判断替代服务端授权。
- URL 前进、后退、刷新和 query 状态与真实请求保持一致。

### 不包含内容

T05 不包含：

- 页面结构、Vue 表现组件、CSS、Tailwind、主题、颜色、间距、布局或动画修改。
- Dialog、Card、Carousel、Table、Drawer、Form 或交互流程重新设计。
- 通过隐藏控件、修改视觉状态或改变文案布局解决接口问题。
- 新增 T04 未定义的业务规则。
- 修改 T04 数据语义来迁就错误的前端接线。
- T06 的最终性能、安全、迁移、部署和发布验收。

允许修改范围优先限于 API client、composable、store、adapter、DTO mapper、请求生命周期、测试和配置。若必须触及表现文件，必须先停止并与用户 brainstorm。

### 依赖关系

T05 必须等待 T04 的 Contract、迁移、权限、公开过滤、错误语义和分页语义稳定。T05 不得与 T04 同时修改同一未冻结 API Contract。

T05 完成后必须向 T06 移交真实 API 接线矩阵、Mock 清理证明、公开/管理流程矩阵、错误和权限验证、视觉对照、构建产物和已知问题。

### 验收标准

- 生产入口不再依赖 Mock、fixture 或业务 localStorage。
- 公开端和管理端关键流程全部使用真实 API。
- 搜索 debounce、IME、cursor、URL、详情深链、分享和前进后退正确。
- 管理端分页、筛选、排序、上传、回收站、板块和冲突流程正确。
- Loading、空、错、重试、权限和会话失效状态准确。
- 适配层有正常、空、错误、超时、冲突、取消和 malformed DTO 测试。
- Playwright 关键公开/管理流程通过。
- 正式项目与 prototype v2 的关键视觉截图保持一致。
- 未修改 T03 表现层和用户交互流程。

## 32.6 T06：系统加固与发布验收

### 任务名称

```text
06 system-hardening-release
```

中文名称：系统加固与发布验收。

### 任务目标

对 T04 后端和 T05 全栈适配后的完整系统执行最终质量门禁，完成回归、自动化测试、性能、安全、迁移、部署、回滚、文档和发布验收。

### 工作范围

#### 测试和回归

- 运行并补齐 Vitest、Workers Vitest 和 Playwright。
- 验证公开端默认首页、搜索、标签、板块、目录、详情、分享、点赞、提交和不可公开数据隔离。
- 验证管理端登录、分页、筛选、排序、编辑、上传、回收站、永久删除和板块管理。
- 验证桌面、平板、手机、浅色、深色、键盘、焦点、reduced motion 和无横向溢出。
- 建立 prototype v2 与正式项目的视觉回归基线。
- 固定测试时间、时区、小时随机槽位、主题、浏览器和数据夹具。

#### 性能、安全和可靠性

- 检查公开列表 cursor、管理分页 50 条、发现新群 10 条、标签聚合和板块 N+1。
- 检查首屏主题、图片懒加载、请求取消、重复请求和生产 bundle 中的 Mock。
- 检查认证、CSRF、权限、输入校验、版本冲突、速率限制、公开过滤和 R2 资源安全。
- 验证位置更新、回收站关联清理和永久删除不会产生部分更新或数据泄露。
- 保留失败 trace、日志、截图、query 记录和安全检查证据。

#### 迁移、部署和发布

- 执行空库、现有库、重复迁移、中断重试和失败恢复演练。
- 验证 D1、R2、Analytics、环境变量、健康检查、日志和监控。
- 编写部署、备份、回滚、失败升级处置和发布后观察 runbook。
- 形成 acceptance.md，记录每项验收结果、命令、证据、阻塞级别和复测结果。

### 不包含内容

T06 不包含：

- 重新设计 prototype v2 或任何前端表现层。
- 修改页面、Vue 组件、CSS、布局、主题、Dialog、动画或用户交互流程。
- 以“优化”“重构”或“回归修复”为理由的大规模改写。
- 删除失败测试、降低断言或更新截图基线来制造通过。
- 新增未经批准的产品需求。
- 在未完成迁移、回滚、安全和关键流程验收前发布。

发现前端表现差异时，只记录问题、截图、风险和建议；后端问题回派 T04，adapter/请求问题回派 T05，表现问题交由用户决定。

### 依赖关系

T06 必须等待 T04 和 T05 完成，并取得两者的交付报告、构建产物、测试结果、迁移说明、已知问题和视觉对照。T06 是发布前最后一个实施任务，总任务负责最终发布判断。

### 验收标准

- Vitest、Workers Vitest、Playwright 和必要的视觉/无障碍检查通过。
- 公开端和管理端完整流程通过，现有提交、点赞、认证、上传、回收站、永久删除和 R2 清理无回归。
- 没有公开数据泄露、权限绕过、CSRF 绕过、重复位置、部分更新或不可恢复迁移。
- 性能检查确认没有明显 N+1、全量返回、重复请求或无界 DOM。
- 迁移、部署、健康检查和回滚均完成可重复演练。
- acceptance.md、发布清单、环境变量、监控、遗留问题和最终结论齐全。
- 没有未经批准的前端表现层改动。

## 32.7 统一执行纪律

所有 T04-T06 在开始实施前必须：

1. 重新完整读取本总 PRD。
2. 读取当前任务的 prd.md、design.md 和 implement.md。
3. 读取相关 .trellis/spec。
4. 压缩上下文后再开始当前任务。
5. 以高级产品经理、Staff Engineer 和 QA 负责人的联合视角 Review 子任务 PRD。
6. 检查当前工作区，保护 T03 和其他用户改动。
7. 对无法在当前边界解决的问题与用户 brainstorm。
8. 未经批准不得改变前端冻结规则。

所有任务必须保留自动化测试、失败证据、变更说明和交接文档。任何任务不得因为依赖尚未稳定而自行复制接口语义或临时扩大范围。

## 32.8 任务完成和移交

T04 完成以服务端 Contract、迁移、API、安全和测试移交为准。T05 完成以真实 API 接线、Mock 清理、公开/管理流程和视觉冻结验证为准。T06 完成以全量质量门禁、迁移部署演练、发布文档和 acceptance.md 为准。

任务完成不等于自动发布。总任务必须对照第 30 章全局验收标准，并由用户确认后才能进入发布决策。

# 33. 子任务依赖关系（重规划版）

当前有效依赖关系为：

```text
T01 基础工程与后端基线 ─┐
T02 prototype v2 视觉基线 ─┼─> T03 正式项目迁移与已有业务接入
                          │
                          └─> T04 后端能力扩展
                                   │
                                   ▼
                            T05 全栈业务适配
                                   │
                                   ▼
                          T06 系统加固与发布验收
                                   │
                                   ▼
                             总任务最终验收
```

T03 由独立任务维护，T04-T06 不得修改或打扰其工作区。T04 可以在 T03 保持独立进行的情况下推进后端规划和实施，但正式 API 接线必须尊重 T03 已交付的视觉和交互基线。T05 必须等待 T04 Contract 和后端能力稳定。T06 必须串行等待 T04、T05 的完整交付。

T04、T05、T06 不允许回到旧 T04-T10 任务边界，也不允许重新创建已删除的子任务 PRD。总任务只负责依赖协调、跨任务问题、最终测试汇总和发布判断。

# 34. 总任务最终验收流程

总任务在所有子任务完成后执行：

1. 对照本总 PRD 检查范围
2. 对照各子任务 PRD 检查交付物
3. 检查所有 migration
4. 检查共享 Zod Contract
5. 检查安全模式
6. 检查状态转换
7. 检查公开端数据过滤
8. 检查桌面和手机布局
9. 检查浅色和深色模式
10. 运行全部 Vitest
11. 运行全部 Workers Vitest
12. 运行全部 Playwright
13. 运行视觉回归
14. 手动验证关键流程
15. 检查 Git 工作区和提交记录
16. 记录剩余非阻塞问题
17. 形成最终 `acceptance.md`

任何子任务未完成、测试失败或公开端泄露下架数据时，总任务不得标记完成。
