# 人工测试清单

> 在本地开发环境 (`pnpm dev`) 或部署环境中按顺序执行，逐项打勾确认。

## 环境准备

- [ ] `pnpm install` 成功
- [ ] `pnpm db:migrate:local` 成功
- [ ] `.dev.vars` 已配置（`ADMIN_PASSWORD` 已设置）
- [ ] `pnpm dev` 启动成功，浏览器可访问 http://localhost:5173

---

## 一、公开首页

### 1.1 首页加载

- [ ] 访问 `/`，页面正常渲染，头部显示"来个群号"
- [ ] 如有已发布群聊，以卡片网格形式展示
- [ ] 暂无群聊时显示"暂无群聊"提示

### 1.2 群聊卡片

- [ ] 卡片包含：标题、平台名称、性质徽章（官方群/同好群）
- [ ] `published` 状态无特殊标记，`delisted` 显示"已下架"红色标签
- [ ] 有标签的群聊显示标签列表
- [ ] 有 Logo 的显示图片，无 Logo 显示"无图"占位

### 1.3 加群操作

- [ ] 有 `group_number` 的卡片显示"复制群号"按钮
- [ ] 点击"复制群号"，弹出 Toast 提示"已复制群号"
- [ ] 有 `url` 的卡片显示"打开链接"按钮，点击在新标签页打开

### 1.4 搜索

- [ ] 搜索框中输入关键词，结果实时过滤
- [ ] 输入不存在的关键词，显示"暂无群聊"
- [ ] 清空搜索框，恢复完整列表
- [ ] URL 中的 `?q=` 参数与搜索词同步

### 1.5 分页

- [ ] 群聊超过 24 个时，滚动到底部自动加载更多
- [ ] 加载时显示骨架屏

### 1.6 点赞

- [ ] 未点赞的卡片显示🤍，点击后变为❤️，计数 +1
- [ ] 再次点击❤️，变回🤍，计数 -1
- [ ] 刷新页面后，点赞状态恢复（localStorage 持久化）
- [ ] 不同浏览器打开同一页面，各自点赞独立

---

## 二、提交群聊

### 2.1 打开提交弹窗

- [ ] 点击"提交新的群聊"按钮，弹出表单弹窗
- [ ] 弹窗包含：标题、群聊性质、平台、群号、HTTPS 链接、标签、简介、联系方式

### 2.2 表单校验

- [ ] 不填标题直接提交，提示必填错误
- [ ] HTTPS 链接输入 `http://unsafe.com`，提示仅允许 HTTPS
- [ ] 标签输入超过 5 个（逗号分隔），提示错误

### 2.3 成功提交

- [ ] 填写完整的有效表单并提交
- [ ] 显示"提交成功！管理员审核后即可公开展示"
- [ ] 关闭弹窗

---

## 三、管理登录

### 3.1 登录页

- [ ] 访问 `/admin/login`，显示管理员登录表单
- [ ] 输入错误密码，显示"Invalid credentials."
- [ ] 连续 6 次错误，返回 429 限流提示

### 3.2 登录成功

- [ ] 输入 `.dev.vars` 中设置的正确密码
- [ ] 自动跳转到 `/admin` 管理后台

### 3.3 退出登录

- [ ] 点击"退出"按钮，跳转回 `/admin/login`
- [ ] 再次访问 `/admin`，自动跳转到登录页

---

## 四、管理员群聊管理

### 4.1 群聊列表

- [ ] 访问 `/admin`，显示群聊表格
- [ ] 表格包含：标题、性质、状态、平台、点赞数、操作
- [ ] 默认只显示未删除的群聊

### 4.2 状态筛选

- [ ] 下拉选择"待审核"，仅显示 pending 群聊
- [ ] 选择"已发布"，仅显示 published 群聊
- [ ] 选择"全部状态"，显示所有群聊

### 4.3 新建群聊

- [ ] 点击"新建群聊"，弹出表单
- [ ] 填写标题、性质、平台，提交
- [ ] 新群聊出现在列表中，状态为"待审核"

### 4.4 编辑群聊

- [ ] 点击某群聊的"编辑"按钮，弹出编辑表单
- [ ] 修改标题，修改状态为"已发布"，提交
- [ ] 列表中该群聊标题和状态已更新

### 4.5 软删除

- [ ] 点击某群聊的"删除"按钮
- [ ] 该群聊从列表中消失
- [ ] 勾选"回收站"，该群聊出现在回收站列表中

### 4.6 恢复与永久删除

- [ ] 在回收站中，点击"恢复"，群聊回到正常列表
- [ ] 再次删除进入回收站，点击"永久删除"
- [ ] 确认后该群聊从回收站中消失

---

## 五、仪表盘

### 5.1 导航

- [ ] 在管理后台顶部点击"运行数据" Tab
- [ ] 显示健康状态面板 + 业务指标面板 + 流量分析面板

### 5.2 健康检查

- [ ] 健康面板显示 API/D1/R2 状态指示（绿灯/红灯）
- [ ] 显示版本号

### 5.3 业务指标

- [ ] 显示四种状态计数（待审核/已发布/已拒绝/已下架）
- [ ] 显示总点赞数、近 7 天新增提交数
- [ ] 如果有高赞群聊，显示 Top 5 排名

### 5.4 流量分析

- [ ] 默认显示 7d 范围
- [ ] 点击 24h/30d 按钮切换范围
- [ ] 未配置 Analytics Token 时显示"暂无数据"

---

## 六、图片上传

### 6.1 上传界面

- [ ] 在群聊编辑表单中，能看到 Logo 上传区域
- [ ] 拖拽一个 JPG/PNG/WebP 文件到上传区，显示预览和尺寸/体积
- [ ] 超过 100KB 的 Logo 显示红色提示并禁用上传

### 6.2 上传流程

- [ ] 上传一个 ≤100KB 的图片，预览正常显示
- [ ] 保存群聊后，Logo 信息已关联

---

## 测试结果记录

| 日期 | 测试人 | 环境 | 通过/失败 | 备注 |
| ---- | ------ | ---- | --------- | ---- |
|      |        |      |           |      |

### 发现问题

| #   | 问题描述 | 严重程度 | 状态 |
| --- | -------- | -------- | ---- |
| 1   |          |          |      |
